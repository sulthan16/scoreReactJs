// import { fromJS } from 'immutable';
// import { makeSelectUserPageDomain } from '../selectors';

// const selector = makeSelectUserPageDomain();

describe('makeSelectUserPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
